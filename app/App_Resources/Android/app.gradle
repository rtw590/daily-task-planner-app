// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "daily.task.planner"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
